const functions = require('../js/common');
/*
describe('CheckAlive', () => {
  test('check for bacteria dead', () => {
    expect(functions.bacteria.checkAlive(false)).toBeFalsy();
  });
  test('check for bacteria alive', () => {
    expect(functions.bacteria.checkAlive(true)).toBeTruthy();
  });
});
*/
